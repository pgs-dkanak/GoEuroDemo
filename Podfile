# Uncomment this line to define a global platform for your project
platform :ios, '8.0'
use_frameworks!

target 'GoEuroDemo' do

pod 'RxSwift'
pod 'Alamofire'

# we should switch to below frameworks to make it compatible with iOS 7.0
# pod 'AFNetworking', '3.1.0'
# pod 'ReactiveCocoa', '2.5'

end

target 'GoEuroDemoTests' do

pod 'RxSwift'
# we should switch to below frameworks to make it compatible with iOS 7.0
# pod 'ReactiveCocoa', '2.5'

end
